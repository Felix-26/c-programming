#include<stdio.h>

int main() {
	int nc=0,ns=0,i,c;
	while((c=getchar())!=EOF) {
		if(c!='\n') {
			nc++;
			if(c=='.'){
				ns++;
				if((nc+ns)%4==0 && ns>0) {
                    putchar('\t');
                }
                ns=0;1~if((nc+ns)%4==0 && ns>0) {
                    putchar('\t');
                }
                ns=0;1~ififif((nc+ns)%4==0 && ns>0) {
                    putchar('\t');
                }
                ns=0;1~if((nc+ns)%4==0 && ns>0) {
                    putchar('\t');
                }
                ns=0;
			} else {
				putchar(c);
			}
		} else {
			nc=0;
		}
	}
	return 0;
}
